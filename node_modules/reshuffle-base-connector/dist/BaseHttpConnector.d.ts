import { Request, Response, NextFunction } from 'express';
import { BaseConnector } from './';
declare class BaseHttpConnector<ConfigOptionsType = any, EventOptionsType = any> extends BaseConnector {
    handle(req: Request, res: Response, next: NextFunction): Promise<boolean>;
}
export default BaseHttpConnector;
